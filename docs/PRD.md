# IIMAKE EAM 控制台 - 产品需求文档（PRD）

> 唯一权威需求来源 | 状态: 🔴 待开发 / 🟡 开发中 / 🟢 已完成 / ⚫ 已废弃

## 功能总览

| 编号 | 功能 | 状态 | 关联设计稿 | 备注 |
|---|---|---|---|---|
| F001 | 设备二维码生成（桌面端） | 🟢 已完成 | [设计稿](./specs/2026-04-29-equipment-qrcode-design.md) | 弹窗 + 下载 PNG + 打印贴纸 + 批量打印 |
| F002 | PDA 扫码 H5 落地页 | 🟢 已完成 | [设计稿](./specs/2026-04-29-equipment-qrcode-design.md) | /m/* 5 个工单入口 + 7 天 token，纯前端 demo |
| F003 | CNC 实时监控墙 | 🔴 待开发 | [设计稿](./specs/2026-04-29-cnc-monitor-wall-design.md) | v1.1 推进 |
| F004 | ERP 主数据只读化 + 设备管理菜单精简 | 🟢 已完成 | — | 设备/备件/工器具主数据由金蝶 ERP 单向推送（ERP→MOM），隐藏 MOM 端新增/手动同步/删除入口；菜单精简 |
| F005 | 采购管理统一抽象（配置驱动） | 🟢 已完成 | — | 设备采购/自制设备备件采购/工器具采购/备件采购四类统一为 `PurchaseListPage` + `purchase-config`；消除重复代码，新增类型=加一份 config |
| F006 | 入库手工入口收口 | 🟢 已完成 | — | 备件入库去掉「手动入库」；工器具入库「新增」改「从 ERP 采购单一键入库」，对齐 F004 单向推送 |
| F007 | 项目/物料主数据对齐金蝶 | 🟢 已完成 | — | 采购单「项目」改为从项目主数据搜索的下拉；备件档案补金蝶物料编码 + 物料属性/产品归属/项目编号 |
| F008 | 备件出入库手动新增 + 出入库类型区分 | 🟢 已完成 | — | 入库：去「从ERP采购单一键入库」→「新增」弹窗(退库入库/调拨入库；ERP采购入库由接口自动写入、不在此新增)，加入库类型列+筛选，删关联采购单/供应商字段；出库：「日常领用登记」→「新增」(日常领用/调拨出库)，类型规整为 维修/保养工单出库·日常领用·调拨出库，移除在建工程出库。**部分修正 F006**：备件入库重新提供人工入口，仅限非ERP的退库/调拨 |
| F009 | 非标设备研制改版：项目管理 + 项目知识库 | 🟢 已完成 | — | 菜单收为两项；8 阶段甘特图（对齐《自动化项目计划统计表》）+ 阶段推进（必传资料校验、缺失可填理由强推）+ 到期前 2 天提醒；项目详情为页内全页视图（阶段管理 / 异常履历 两页签，阶段资料上传弹窗后自动归档知识库，不设单独资料页签）（对齐《设备异常履历表》）；知识库自由文件夹树 + 阶段资料自动归档；纯前端 localStorage 持久化 |
| F010 | 非标设备研制迭代：甘特图冻结表头 + 阶段可插入 + 异常闭环流程 | 🟢 已完成 | F009 | 甘特图表头纵向冻结、时间轴日/周竖向网格线；阶段表任意行下插入新阶段（序号重排并同步位移 currentStage / issues / 知识库 stageIdx）；异常类型改用户可维护字典；责任人改「阶段责任人」随阶段自动带出 + 新增「处理人」下拉；异常履历升级为四态闭环状态机（待处理→已处理待确认/已优化待确认→已结案，支持打回返工）+ 流转记录时间线 + 四态计数筛选 |

## 变更历史

- 2026-04-29: F001 + F002 demo 完成（前端 + mock 闭环，无后端）
- 2026-06-08: F004 完成 — 基于「主数据由金蝶 ERP 单向推送」原则迭代设备管理 demo：
  - 菜单：设备基础数据精简为 4 项（移除 设备状态字典/故障代码字典/维保类型字典/点检项库）；采购到货看板移至采购管理末位；系统管理移除「ERP 同步配置」菜单
  - 设备档案：移除 新增/手动同步 ERP（按钮+弹窗）/批量删除/行内删除，保留 查看/编辑/二维码/批量打印
  - 备件档案：移除 新增/行内删除，保留编辑
  - 工器具档案：禁用 新增/删除（enable-create/enable-delete=false），保留编辑
  - 已部署 3010 + 推送 GitHub（commit 9606b6f，dev_2026.3.1）
- 2026-06-09: F005 + F006 完成 — 采购管理整体抽象 + 入库入口收口：
  - F005 采购统一抽象（方案 A·配置驱动）：新增 `_purchase-shared/PurchaseListPage.vue`（统一组件）+ `types.ts` + `configs/{_shared,equipment,diy,tooling,sparePart}.ts`；4 个 `page.vue` 瘦身为壳（1602 行 → 共约 30 行）
    - 设备采购：去掉钣金/机加/外购三 Tab → 单张「采购明细」扁平表；项目名称改为选填；历史三清单数据经 `mapRow` 归并展示
    - 自制设备备件采购：头部字段对齐设备采购（通用设备采购头）+ 保留钣金/机加/外购三 Tab；mock 数据按「项目+三清单」重塑
    - 工器具采购：旧自闭环审批流（提交审批/审批进度时间线/重试ERP/PO详情）统一为飞书黑盒流程（推送飞书/刷新状态/重试推送）
    - 备件采购：纳入统一组件，行为不变
  - F006 入库入口：备件入库去「手动入库」按钮+弹窗；工器具入库「+新增」→「从 ERP 采购单一键入库」（SimpleListPage 加 createText/createIcon 可选属性）
  - 验证：`vue-tsc --noEmit` 本次文件零错误；`build:mock` 构建成功
- 2026-06-09: F007 完成 — 项目/物料主数据对齐金蝶（最小范围）：
  - 项目：新增 `configs/project-master.ts` 项目主数据（编码/名称/数据状态/禁用/创建修改信息，样例 RB11=GO2机器狗 等）；统一组件 `PurchaseListPage` 加 `projectSelect` 字段类型，采购单「项目」改为可搜索下拉（选中回填项目名称）；单据号字段标签由「项目编号」改为「单据编号」以消歧
  - 物料：备件档案（eamBaseMaterial）展示层经 `enrichMaterial` 补「金蝶物料编码（点分层级码）+ 物料属性(自制/外购) + 产品归属(C端/B端/通用) + 项目编号」；备件档案列表/表单补对应字段；原始 `number=BJ-XXX` 不动，避免破坏入库/出库/预警按备件号的交叉引用
  - 验证：`build:mock` 构建成功
- 2026-06-17: F008 完成 — 备件出入库手动新增 + 出入库类型区分：
  - 入库（sparePartInbound）：去「从 ERP 采购单一键入库」按钮 → 改「新增」弹窗（入库类型：退库入库 / 调拨入库；ERP采购入库由接口实时自动写入，不在此页面人工新增，对齐 F004 单向推送）；表格「单据号」后加「入库类型」列 + 查询筛选；删除弹窗内 关联采购单 / 供应商 字段（采购专属，对退库/调拨无意义）
  - 出库（sparePartOutbound）：「日常领用登记」→ 改「新增」弹窗（出库类型：日常领用 / 调拨出库）；出库类型规整为 维修工单出库 / 保养工单出库 / 日常领用 / 调拨出库；删除关联单号字段；移除「在建工程出库」
  - 背景：机加来料/成品检验需区分来源；机加件识别最终方案 = WMS「数控加工仓」仓库类型（属 QMS/WMS 蓝图范畴，见 QMS 业务蓝图）
  - 验证：`build:mock` 构建成功；已部署 8.130.182.148:3010 + 推送 GitHub（commit 6bcc827，dev_2026.3.1）
- 2026-09-04: F009 完成 — 非标设备研制改版（设计：docs/plans/2026-09-04-npi-project-management-design.md）：
  - 菜单：项目总览/方案评审/BOM/装配与验收 四页删除 → 项目管理 / 项目知识库；`npiOverview` 重定向到 `npiProject`
  - 项目管理：自研 div 甘特（周/月粒度、今日线自动定位、临期橙/逾期红）+ 列表；8 阶段模板（方案设计+评审→细化出图→BOM整理下单→物料采购→整机装配→整机布线→上电调试→交付导入），阶段名/责任人/起止/进度可改；推进校验必传资料，缺失可填理由强推并记阶段日志；提醒条统计 2 天内到期/已逾期
  - 异常履历：项目详情页签，按阶段记 问题/原因/临时措施/长期措施/异常类型/责任人/发生·计划·实际完成日期/结案
  - 知识库：`TreeListLayout` 文件夹树（新建/重命名/删除，非空与只读不可删）+ 文件列表（递归含子夹）；阶段上传自动归档到「项目资料/项目名/阶段名」只读目录
  - 数据：Pinia `useNpiStore` + localStorage `eam-npi-store-v1`，种子取自附件 14 个项目；「重置演示数据」恢复种子；文件只记元数据不真存
  - 验证：`vue-tsc` NPI 文件零报错；`build:mock` 成功；Playwright 走通 提醒→详情→强推→上传归档→知识库→重置 全链路（commit e17565a，dev_2026.3.1）；2026-09-04 已部署 8.130.182.148:3010（备份 yushu-eam-backup-20260904-202240）+ 推送 GitHub f8ec67c
- 2026-09-09: F010 完成 — 非标设备研制迭代（设计：docs/plans/2026-09-09-npi-issue-workflow-design.md）：
  - 甘特图：`.gantt-scroll` 改容器内纵向滚动，两行表头 sticky 冻结（top 0 / 30px）；时间轴加日线+周线两层背景网格，tile 精确到一个周期并平移对齐周一，与表头刻度画在同一位置（每天左边缘）；月粒度下日线转透明只留周线
  - 阶段插入：阶段表每行操作列加 `+`，在该行下方插入新阶段。唯一入口 `npiStore.insertStageAfter`，内部整表重排 idx 并同步位移 `project.currentStage`、`issues.stageIdx`、知识库 `folders/files.stageIdx`；阶段数硬编码 8 全部清除（`buildNewStages` 的 `/8`、`advanceStage` 的 `isLast`、`nextStageLabel` → 一律 `stages.length`）；知识库阶段文件夹定位从「拼 `-s{idx}` 的 id」改为「按 projectId + stageIdx 字段查」，否则重排后会重复建夹
  - 异常类型：由联合类型改为 store 字典 `issueTypes`（随数据落 localStorage），页面提供「异常类型维护」弹窗（新增/改名/删除）；改名同步改写引用该类型的异常记录，删除时若已被引用则拒绝
  - 异常表单：「责任人」改「阶段责任人」（随所属阶段自动带出，只读）；新增「处理人」下拉（候选 = 项目负责人 + 各阶段责任人 + 历史处理人，允许直接输入新人员）；新建弹窗移除临时/长期措施两框，措施归到处理/优化环节
  - 异常闭环：状态由 `open/closed` 扩为 `pending / handled / optimized / closed`；处理（填临时措施）→ 已处理待确认，优化（临时+长期）→ 已优化待确认，②可再升级为③；阶段责任人可结案或打回（理由必填，退回①且不清空已填措施）。唯一入口 `transitionIssue`，操作列按钮与 store 校验共用 `ISSUE_ALLOWED_ACTIONS` 同一张表，不手写 v-if 状态判断；每步追加流转记录，表格展开行以时间线展示；工具栏加四态计数筛选条
  - 数据：`NpiIssue` 增 `handler` / `flow`；旧存档 normalize 兼容（`open→pending`，缺 flow 补 create 记录），不升 STORAGE_KEY 以免丢手建项目；演示异常 5 条扩到 10 条铺满四态，含 2 条最长流转链（含打回、含②→③升级）
  - 验证：`vue-tsc` NPI 文件零报错；`eslint` 改动文件 0 error；`build:mock` 成功；Playwright 在本地 preview 与线上 3010 各走通一遍：表头滚动后仍钉顶、插入阶段后三处引用同步位移、完整闭环 create→handle→reject→handle→reject→optimize→close 七条流转记录、字典增删拒删被引用项；⚠️ 单测 `vitest run` 因 vitest@4 与 vite@5 版本冲突启动失败（预先存在，不覆盖 NPI，详见 docs/SHIP-PROFILE.md）
  - 部署：2026-09-09 已部署 8.130.182.148:3010（备份 yushu-eam-backup-20260909-224926）；**使用前需点一次「重置演示数据」**才能看到新演示数据
